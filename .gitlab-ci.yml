image: haskell:latest

before_script:
  - stack init
  - stack setup
  - stack build

pages:
  stage: deploy
  script:
  - stack exec blog build
  artifacts:
    paths:
    - public
  only:
  - master