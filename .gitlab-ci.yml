image: haskell:latest

before_script:
  - stack install hakyll
  - stack setup
  - stack build

pages:
  stage: deploy
  script:
  - stack exec blog build
  artifacts:
    paths:
    - public
  only:
  - master