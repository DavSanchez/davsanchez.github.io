image: haskell:latest

before_script:
  - stack upgrade
  - stack init
  - stack install

pages:
  stage: deploy
  script:
  - stack exec blog build
  artifacts:
    paths:
    - public
  only:
  - master